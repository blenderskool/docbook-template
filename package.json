{
  "name": "{{ name }}",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "docbook build",
    "dev": "docbook dev",
    "start": "npm run dev"
  },
  "license": "MIT",
  "dependencies": {
    "docbook": "^0.5.0"
  }
}
